package ch11;
// 48~51 HashMap(2)
public class Ex_48_49_50_51 {
    public static void main(String[] args) {
        // 48 HashMap - 주요 메서드

        // HashMap() - 생성자 해시맵은 해시테이블에 저장 하는데 해시테이블 = 배열 + 링크드리스트  배열의 초기 용량을 지정 할수 있다
        // HashMap(int initialCapacity) -
        // HashMap(int initalCapeacity, float loadFactor)
        // HashMap(Map m)
        // Object put(Object key, Object value) - 저장
        // void putAll(Map m) - 추가
        // Object remove(Object key) - 삭제
        // Object replace(Object key, Object value) - 변경
        // boolean replace(Object key, Object oldValue, Object newValue) - 변경

        // 읽어 올대
        // Set entrySet() - 키(key) 와 값(value) 쌍으로 묶어서 가져올때
        // Set keySet() -  키(key) 만 가져오는 것
        // Collection values()  - 값(value) 만 가져오는것
        // Object get(Object key)   - 키(key) 를 넣으면 값(value) 를 반환한다
        // Object getOrdefault(Object key, Object defaultValue) - 키(key) 가 없을때 지정한 값을 반환
        // boolean containsKey(Object key)  - 지정한 키(key) 가 있는지
        // boolean containsValeu(Object value)  - 지정한 값(value) 가 있는지

        // int size() - 사이즈
        // boolean isEmpty() - null 체크
        // void clear() - 모두 삭제
        // Object clone() - 복제







    }
}

